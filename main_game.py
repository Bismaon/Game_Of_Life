from distutils.util import change_root
import tkinter as Tk

class GameOfLife:
    def __init__(self):
        self.window=Tk.tk()
        
    def creation_de_gride(self):
        for row in range():
            for column in range():

"""    def neighbor(self, position?):
        find position, neighbors, add it to a global list

    def vivant(self):
        if trois neighbor or deux neighbors:
            vivant, reste non change
        elif plus de trois neighbors or moins de deux neighbors:
            morte"""
            celulle --> widgets --> liste --> matrice --> celulle --> vivante (color noire)/morte(color blanche)

